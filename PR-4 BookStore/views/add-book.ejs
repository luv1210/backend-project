<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Book | Book Haven</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
   :root {
  --primary-color: #0F828C;      /* Teal blue */
  --secondary-color: #f0f9fa;    /* Very light teal for backgrounds */
  --accent-color: #e6f4f5;       /* Light teal accent */
  --text-color: #1a3c40;         /* Dark teal for text */
  --light-bg: #f8fdfd;           /* Off-white with teal tint */
  --hover-color: #0c6c74;        /* Darker teal for hover states */
}

body {
  background-color: var(--light-bg);
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  color: var(--text-color);
}

.navbar {
  background-color: white !important;
  box-shadow: 0 2px 10px rgba(15, 130, 140, 0.08);
}

.navbar-brand {
  font-weight: 600;
  color: var(--text-color) !important;
  letter-spacing: 0.5px;
}

.form-container {
  max-width: 600px;
  margin: 40px auto;
  padding: 30px;
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 20px rgba(15, 130, 140, 0.1);
  border-top: 4px solid var(--primary-color);
}

.form-title {
  color: var(--primary-color);
  text-align: center;
  margin-bottom: 30px;
  font-weight: 600;
  position: relative;
  padding-bottom: 10px;
}

.form-title:after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 80px;
  height: 3px;
  background: var(--primary-color);
}

.form-label {
  font-weight: 500;
  color: var(--text-color);
  margin-bottom: 8px;
}

.form-control {
  padding: 10px 15px;
  border-radius: 8px;
  border: 1px solid #d1e7e9;
  transition: all 0.3s;
  background-color: var(--light-bg);
}

.form-control:focus {
  border-color: var(--primary-color);
  box-shadow: 0 0 0 0.25rem rgba(15, 130, 140, 0.25);
  background-color: white;
}

.btn-primary {
  background-color: var(--primary-color);
  border-color: var(--primary-color);
  color: white;
  font-weight: 500;
  padding: 10px 20px;
  border-radius: 8px;
  transition: all 0.3s;
}

.btn-primary:hover {
  background-color: var(--hover-color);
  border-color: var(--hover-color);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(15, 130, 140, 0.2);
}

.btn-primary:active {
  transform: translateY(0);
}

.form-select {
  padding: 10px 15px;
  border-radius: 8px;
  border: 1px solid #d1e7e9;
  background-color: var(--light-bg);
}

.form-select:focus {
  border-color: var(--primary-color);
  box-shadow: 0 0 0 0.25rem rgba(15, 130, 140, 0.25);
  background-color: white;
}

.genre-options {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
  gap: 10px;
  margin-top: 10px;
}

.genre-option {
  display: flex;
  align-items: center;
}

.genre-option input {
  margin-right: 8px;
}

.fa-book-open {
  color: var(--primary-color);
}

.add-book-btn {
  background-color: white;
  border: 2px solid var(--primary-color);
  color: var(--primary-color);
  font-weight: 500;
}

.add-book-btn:hover {
  background-color: var(--primary-color);
  color: white;
  border-color: var(--primary-color);
}

/* Custom styling for select options */
select option:hover,
select option:focus,
select option:active,
select option:checked {
  background-color: var(--accent-color) !important;
  color: var(--text-color);
}

/* Add some subtle teal accents to various elements */
.form-container input[type="number"] {
  background-color: var(--secondary-color);
}

/* Style for the back button in navbar */
.navbar .btn {
  border-radius: 8px;
  padding: 8px 16px;
  font-weight: 500;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .form-container {
    margin: 20px 15px;
    padding: 20px;
  }
  
  .form-title {
    font-size: 1.5rem;
  }
}
  </style>
</head>

<body>

  <!-- Header -->
  <nav class="navbar navbar-expand-lg navbar-light sticky-top">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-book-open me-2"></i>Book Haven
      </a>
      <a href="/" class="btn btn-primary add-book-btn">
       Back to Books
      </a>
    </div>
  </nav>

  <!-- Form Container -->
  <div class="container py-4">
    <div class="form-container">
      <h2 class="form-title">
        <i class="fas fa-plus-circle me-2"></i>Add New Book
      </h2>
      <form method="post" action="/add-book">
        <div class="mb-4">
          <label for="bookname" class="form-label">Book Title</label>
          <input type="text" class="form-control" id="bookname" name="bookname" required>
        </div>

        <div class="mb-4">
          <label for="authorname" class="form-label">Author</label>
          <input type="text" class="form-control" id="authorname" name="authorname" required>
        </div>

        <div class="row mb-4">
          <div class="col-md-6">
            <label for="publishYear" class="form-label">Publish Year</label>
            <input type="number" class="form-control" id="publishYear" name="publishYear" required>
          </div>
          <div class="col-md-6">
            <label for="price" class="form-label">Price (₹)</label>
            <input type="number" class="form-control" id="price" name="price" required>
          </div>
        </div>

        <div class="mb-4">
          <label class="form-label">Genre</label>
          <select class="form-select" name="genre" required>
            <option value="">Select Genre</option>
            <option value="Science Fiction">Science Fiction</option>
            <option value="Mystery">Mystery</option>
            <option value="Thriller">Thriller</option>
            <option value="Romance">Romance</option>
            <option value="Biography">Biography</option>
            <option value="History">History</option>
            <option value="Business">Business</option>
            <option value="Technology">Technology</option>
          </select>
        </div>

        <div class="row mb-4">
          <div class="col-md-6">
            <label for="language" class="form-label">Language</label>
            <input type="text" class="form-control" id="language" name="language" required>
          </div>
          <div class="col-md-6">
            <label for="bookUrl" class="form-label">Cover Image URL</label>
            <input type="url" class="form-control" id="bookUrl" name="bookUrl"
              placeholder="https://example.com/image.jpg">
          </div>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-save me-2"></i>Save Book
          </button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>