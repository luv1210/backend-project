<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Haven | Premium Book Store</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
  :root {
  --primary-color: #0F828C;      /* Teal blue */
  --secondary-color: #f0f9fa;    /* Very light teal for backgrounds */
  --accent-color: #e6f4f5;       /* Light teal accent */
  --text-color: #1a3c40;         /* Dark teal for text */
  --light-bg: #f8fdfd;           /* Off-white with teal tint */
  --hover-color: #0c6c74;        /* Darker teal for hover states */
  --card-shadow: rgba(15, 130, 140, 0.08);
}

body {
  background-color: var(--light-bg);
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  color: var(--text-color);
}

.navbar {
  background-color: white !important;
  box-shadow: 0 2px 10px rgba(15, 130, 140, 0.05);
}

.navbar-brand {
  font-weight: 600;
  color: var(--text-color) !important;
  letter-spacing: 0.5px;
}

.btn-primary {
  background-color: var(--primary-color);
  border-color: var(--primary-color);
  color: white;
  font-weight: 500;
}

.btn-primary:hover {
  background-color: var(--hover-color);
  border-color: var(--hover-color);
}

.card {
  border: none;
  border-radius: 8px;
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px var(--card-shadow);
  overflow: hidden;
  background: white;
  border: 1px solid rgba(15, 130, 140, 0.1);
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 20px rgba(15, 130, 140, 0.12);
  border-color: rgba(15, 130, 140, 0.2);
}

.card-img-container {
  padding: 20px;
  background-color: var(--secondary-color);
  display: flex;
  justify-content: center;
  align-items: center;
  height: 220px;
}

.card-img {
  max-height: 100%;
  max-width: 100%;
  object-fit: contain;
  border-radius: 4px;
}

.card-body {
  padding: 20px;
}

.card-title {
  font-size: 15px;
  font-weight: 600;
  color: var(--text-color);
  margin-bottom: 6px;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.3;
}

.card-text {
  font-size: 13px;
  color: #666;
  margin-bottom: 10px;
}

.price {
  color: var(--primary-color);
  font-size: 18px;
  font-weight: 700;
}

.action-buttons {
  padding: 12px 20px;
  background-color: var(--secondary-color);
  border-top: 1px solid rgba(15, 130, 140, 0.1);
}

.btn-sm {
  font-size: 12px;
  padding: 5px 10px;
  border-radius: 5px;
}

.rating {
  color: #FFC107;
  font-size: 12px;
  margin-bottom: 8px;
}

.badge-genre {
  background-color: var(--accent-color);
  color: var(--text-color);
  font-weight: 500;
  font-size: 12px;
  padding: 4px 10px;
  border-radius: 12px;
  border: 1px solid rgba(15, 130, 140, 0.2);
}

.container {
  max-width: 1400px;
}

.btn-outline-primary {
  color: var(--primary-color);
  border-color: var(--primary-color);
}

.btn-outline-primary:hover {
  background-color: var(--primary-color);
  color: white;
  border-color: var(--primary-color);
}

.btn-outline-danger {
  color: #dc3545;
  border-color: #dc3545;
}

.btn-outline-danger:hover {
  background-color: #dc3545;
  color: white;
  border-color: #dc3545;
}

.add-book-btn {
  border-radius: 8px;
}

.empty-state {
  text-align: center;
  padding: 60px 20px;
  background-color: white;
  border-radius: 12px;
  box-shadow: 0 4px 12px rgba(15, 130, 140, 0.08);
  margin-top: 30px;
  border: 1px solid rgba(15, 130, 140, 0.1);
}

.empty-state i {
  font-size: 48px;
  color: var(--primary-color);
  margin-bottom: 20px;
}

.empty-state h3 {
  color: var(--text-color);
  margin-bottom: 15px;
}

.empty-state p {
  color: #666;
  max-width: 500px;
  margin: 0 auto 25px;
}

.fa-book-open {
  color: var(--primary-color);
}

.fa-books {
  color: var(--primary-color);
}

.collection-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
  padding-bottom: 15px;
  border-bottom: 1px solid rgba(15, 130, 140, 0.15);
}

.collection-title {
  font-size: 28px;
  font-weight: 600;
  color: var(--text-color);
  display: flex;
  align-items: center;
}

.collection-title i {
  margin-right: 12px;
  font-size: 24px;
}

.collection-count {
  font-size: 14px;
  color: var(--primary-color);
  background: var(--secondary-color);
  padding: 4px 12px;
  border-radius: 20px;
  box-shadow: 0 2px 5px rgba(15, 130, 140, 0.1);
  border: 1px solid rgba(15, 130, 140, 0.2);
}

/* Hover effects for all buttons */
.btn-outline-primary:hover,
.btn-outline-danger:hover {
  transform: translateY(-1px);
  transition: all 0.2s;
}

/* Card image hover effect */
.card-img-container img {
  transition: transform 0.3s ease;
}

.card:hover .card-img-container img {
  transform: scale(1.03);
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .collection-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
  
  .collection-count {
    align-self: flex-start;
  }
  
  .card-img-container {
    height: 180px;
  }
}
  </style>
</head>

<body>

  <!-- Header -->
  <nav class="navbar navbar-expand-lg navbar-light sticky-top">
    <div class="container">
      <a class="navbar-brand" href="#">
        <i class="fas fa-book-open me-2"></i>Book Haven
      </a>
      <a href="/add-book" class="btn btn-primary add-book-btn">
        <i class="fas fa-plus me-1"></i> Add Book
      </a>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container py-4">
    <% if (books.length> 0) { %>
      <div class="collection-header">
        <h2 class="collection-title">
          <i class="fas fa-books"></i>My Book Collection
        </h2>
        <span class="collection-count">
          <%= books.length %>
            <%= books.length===1 ? 'book' : 'books' %>
        </span>
      </div>

      <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 row-cols-xl-5 g-4">
        <% books.forEach(book=> { %>
          <div class="col">
            <div class="card h-100">
              <div class="card-img-container">
                <img src="<%= book.bookUrl || 'https://via.placeholder.com/160x220?text=No+Cover' %>" class="card-img"
                  alt="<%= book.bookname %>">
              </div>

              <div class="card-body">
                <h6 class="card-title">
                  <%= book.bookname %>
                </h6>
                <p class="card-text">by <%= book.authorname %>
                </p>

                <div class="rating mb-2">
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star-half-alt"></i>
                  <span class="text-muted ms-1" style="font-size: 11px;">(24)</span>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-auto">
                  <span class="badge-genre">
                    <%= book.genre %>
                  </span>
                  <span class="price">₹<%= book.price %></span>
                </div>
              </div>

              <div class="action-buttons d-flex justify-content-between">
                <a href="/edit-book/<%= book._id %>" class="btn btn-sm btn-outline-primary">
                  <i class="fas fa-edit me-1"></i>Edit
                </a>
                <a href="/delete-book/<%= book._id %>" class="btn btn-sm btn-outline-danger"
                  onclick="return confirm('Are you sure you want to delete this book?')">
                  <i class="fas fa-trash me-1"></i>Delete
                </a>
              </div>
            </div>
          </div>
          <% }) %>
      </div>
      <% } else { %>
        <div class="empty-state">
          <i class="fas fa-book-open"></i>
          <h3>Your Book Collection is Empty</h3>
          <p>Start building your library by adding your first book. Click the button below to get started.</p>
          <a href="/add-book" class="btn btn-lg btn-primary">
            + Add Book
          </a>
        </div>
        <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>